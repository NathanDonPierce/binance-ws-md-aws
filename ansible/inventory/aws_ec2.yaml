plugin: amazon.aws.aws_ec2
regions:
  - ap-northeast-1
filters:
  tag:Project: ws-project
  instance-state-name: running
keyed_groups:
  - key: ec2_tags.Role
    prefix: tag
compose:
  ansible_host: "(ec2_tags.Role == 'ansible') | ternary(public_ip_address, private_ip_address)"
